export type MinMaxValues = {
  age: { min: number, max: number },
  distance: { min: number, max: number },
  acceptedOffers: { min: number, max: number },
  canceledOffers: { min: number, max: number },
  replyTime: { min: number, max: number }
};